import React, { Component } from 'react'

class Form extends Component {
  render() {
    return (
      <form className="form">
        <input className="input" />
        <button>Add</button>
      </form>
    )
  }
}

export default Form
